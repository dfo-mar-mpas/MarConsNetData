% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_eDNA.R
\name{data_eDNA}
\alias{data_eDNA}
\title{Build a cleaned eDNA sampling dataset from the raw eDNA-for-MPAs directory}
\usage{
data_eDNA()
}
\value{
A data frame containing:
\describe{
\item{ID}{Sample identifier}
\item{date}{Sampling date (class \code{Date})}
\item{latitude}{Sample latitude}
\item{longitude}{Sample longitude (corrected to negative values)}
\item{species_richness}{Count of unique species detected in the sample}
\item{method}{eDNA sampling method inferred from folder structure}
\item{location}{Sampling location identifier}
\item{year}{Extracted sampling year}
}
}
\description{
This function recursively scans the \verb{../eDNA-for-MPAs/data/} directory to
locate all CSV files containing \code{"GOTeDNA"} in their file name or path.
Files are grouped by cruise mission, metadata and data files are paired,
naming inconsistencies are corrected, and a unified table of sample-level
information is produced.
}
\details{
The function performs several formatting corrections, including:
\itemize{
\item resolving inconsistent \code{materialSampleID} naming (e.g., \code{X}, \verb{_}, \code{-})
\item handling metadata files with different column name conventions
\item resolving location name inconsistencies (e.g., \code{ESI}, \code{SAB})
\item extracting sample dates, coordinates, richness, and method type
\item correcting positive longitudes by flipping sign
}

The final result is a single cleaned data frame with one row per sample.

The function assumes the following:
\itemize{
\item The directory \verb{../eDNA-for-MPAs/data/} exists and contains cruise folders.
\item Metadata files contain either \code{materialSampleID}, \code{SampleID}, or \code{eDNA_Tube}.
\item Species columns in data files may be named \code{Species}, \code{species}, or \code{V6}.
\item Latitude/longitude may be stored as \code{decimalLatitude}/\code{decimalLongitude}
or \code{Lat}/\code{Long}.
}

The function prints progress messages (\code{i}, \code{j}, \code{k}, \code{l}) and may enter
\code{browser()} in unexpected or inconsistent cases.
}
\examples{
\dontrun{
  df <- data_eDNA()

  # Quick map preview
  leaflet::leaflet(df) \%>\%
    leaflet::addTiles() \%>\%
    leaflet::addCircleMarkers(
      lat = ~latitude,
      lng = ~longitude,
      popup = paste("Lat:", df$latitude, "<br>Lon:", df$longitude)
    )
}

}

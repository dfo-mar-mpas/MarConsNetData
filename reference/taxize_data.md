# Group species into taxonomic groups

This function groups species into taxonomic groups. It is used in the
`taxize=TRUE` argument of the `get_project_data` function.

## Usage

``` r
taxize_data(df = NULL)
```

## Arguments

- df:

  data frame to be taxized
